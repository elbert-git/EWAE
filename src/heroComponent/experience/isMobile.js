const parser = new UAParser();
const os = parser.getOS().name;

export default function isMobile(){
    // if(os === 'Android' || os === 'iOS'){return true}
    // else{return false}
    return false // mobile version is visually ugly and i can't solve it
}